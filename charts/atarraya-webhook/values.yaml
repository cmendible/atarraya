# Default values for atarraya-webhook.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: cmendibl3/atarraya-webhook
  pullPolicy: Always

